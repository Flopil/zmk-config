/ {
    chosen {
        zmk,rgb-underglow = &myleds;
    };

    myleds: leds {
        compatible = "worldsemi,ws2812-spi";
        spi-dev = <&spi3>;
        chain-length = <78>;
        color-mapping = <1 0 2>;
    };
};