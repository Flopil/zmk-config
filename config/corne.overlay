/ {
    chosen {
        zmk,rgb-underglow = &myleds;
        zmk,rgb-keyboard = &myleds;
    };

    myleds: leds {
        compatible = "worldsemi,ws2812-spi";

        spi-dev = <&spi3>;

        chain-length = <78>;

        color-mapping = <1 0 2>;
    };
};

&spi3 {
    status = "okay";
};