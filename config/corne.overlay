/ {
    chosen {
        zmk,underglow = &underglow;
    };

    underglow: underglow {
        compatible = "zmk,rgb-underglow";

        chain-length = <27>;

        out-gpios = <&gpio0 6 GPIO_ACTIVE_HIGH>;
    };
};